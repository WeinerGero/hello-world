apiVersion: v1

kind: Service

metadata:
  name: service
  labels:
    app: service

spec:
  type: ClusterIP
  selector:
    app: hello-world
  ports:
    - protocol: TCP
      port: 80
      targetPort: 32777
